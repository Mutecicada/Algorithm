#include <string>
#include <vector>
#include <algorithm>

using namespace std;

string solution(string s) {
	string answer = "";

	vector<char> a;
	copy(s.begin(), s.end(), a);
//	sort(a.begin(), a.end());
//	copy(a.begin(), a.end(), answer);

	return answer;
}

int main()
{
	solution("Zbcdefg");
}